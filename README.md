必要操作

· Change 'Docker_with_JDK.yml' file "hosts" and  "roles_path" parameter

· RPM package that needs to be imported into JDK into "roles/jdk/files/" directory

· Run 'ansible-playbook Docker_with_JDK.yml'
